# The .env file should be generated during the deploy action
# from pre-saved repository secrets and variables
JWT_SECRET_KEY="very-big-secret" # should be generated with `openssl rand -base64 32`
DATA_TOKEN="very-secret-data-token"  # should be generated with `openssl rand -base64 32`
NEXT_BASE_URL="http://localhost:3000"