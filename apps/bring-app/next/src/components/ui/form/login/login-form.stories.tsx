import type {Meta} from "@storybook/react";

import LoginForm from "./login-form";

const meta = {
	title: "Components/Ui/LoginForm",
	component: LoginForm,
	tags: ["autodocs"],
} satisfies Meta<typeof LoginForm>;

export default meta;
